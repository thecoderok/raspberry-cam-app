@model System.Collections.Generic.List<RpiServerApp.Data.WebcamCapture>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>title</h2>

@foreach (var entry in Model)
{
    <img src="/Home/Image?file=@entry.Location"/>
    <br>
}
